[package]
name = "patina-ir"
version = "0.1.0"
edition = "2024"
description = "Core intermediate representation for Patina Scheme compiler"

[dependencies]
# Core types (foundation crate)
patina-core = { path = "../patina-core" }

# Error handling
thiserror = { workspace = true }

[lib]
name = "patina_ir"
path = "src/lib.rs"
